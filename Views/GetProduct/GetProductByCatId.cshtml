@using Ecom.Models


@{
    
   // List<Category> products = ViewBag.category;
    List<Product> products = ViewBag.getProduct;
    ViewBag.Title = "GetProductByCatId";
    Layout = "~/Views/Shared/_LayoutPage1.cshtml";
}
@model List<Product>
    <h2>GetProductByCatId</h2>
    <html>
    <head>
        <meta name="viewport" content="width=device-width" />
        <title>ViewProduct</title>
    </head>
    <body>
        <h3> View Product </h3>
        <div>

            <table class="table table-bordered table-striped table-hover">
                <tr>

                    <th>Name</th>

                    <th>Description</th>

                </tr>
                @*@foreach (Product product in products)
                {
                    <tr>
                        <td>@product.Name</td>

                        <td>@product.Description</td>


                    </tr>
                }*@
            </table>

        </div>
    </body>
</html>

